diff --git a/casatools/src/code/synthesis/TransformMachines2/AWVisResampler.cc b/casatools/src/code/synthesis/TransformMachines2/AWVisResampler.cc
--- a/casatools/src/code/synthesis/TransformMachines2/AWVisResampler.cc
+++ b/casatools/src/code/synthesis/TransformMachines2/AWVisResampler.cc
@@ -34,9 +34,12 @@
 #include <casacore/coordinates/Coordinates/CoordinateSystem.h>
 #include <casacore/casa/OS/Timer.h>
+#include <cstdint>
+#include <cstdlib>
+#include <cstring>
 #include <fstream>
-#include <iostream>
-#include <typeinfo>
 #include <iomanip>
+#include <iostream>
+#include <typeinfo>
 #include <cfenv>
 //#include <synthesis/TransformMachines2/FortranizedLoops.h>
 //#include <synthesis/TransformMachines2/hpg.hpp>
diff --git a/casatools/src/code/synthesis/TransformMachines2/accumulateFromGrid.inc b/casatools/src/code/synthesis/TransformMachines2/accumulateFromGrid.inc
--- a/casatools/src/code/synthesis/TransformMachines2/accumulateFromGrid.inc
+++ b/casatools/src/code/synthesis/TransformMachines2/accumulateFromGrid.inc
@@ -5,6 +5,28 @@
 //
 //--------------------------------------------------------------------------------
 {
+  // Bounded VLASS parity diagnostic. This is inert unless the output path is
+  // supplied and records exactly one requested GridToData footprint.
+  static bool vlassDegridTraceDone = false;
+  const char* vlassDegridTracePath = std::getenv("CASA_VLASS_DEGRID_PREFIX_TRACE");
+  const char* vlassDegridTraceRowText = std::getenv("CASA_VLASS_DEGRID_TRACE_ROW");
+  const char* vlassDegridTraceChannelText = std::getenv("CASA_VLASS_DEGRID_TRACE_CHANNEL");
+  const char* vlassDegridTracePolText = std::getenv("CASA_VLASS_DEGRID_TRACE_POL");
+  const char* vlassDegridTraceMColText = std::getenv("CASA_VLASS_DEGRID_TRACE_MCOL");
+  const Int vlassDegridTraceRow =
+      vlassDegridTraceRowText == nullptr ? 0 : std::atoi(vlassDegridTraceRowText);
+  const Int vlassDegridTraceChannel =
+      vlassDegridTraceChannelText == nullptr ? 11 : std::atoi(vlassDegridTraceChannelText);
+  const Int vlassDegridTracePol =
+      vlassDegridTracePolText == nullptr ? 0 : std::atoi(vlassDegridTracePolText);
+  const Int vlassDegridTraceMCol =
+      vlassDegridTraceMColText == nullptr ? 0 : std::atoi(vlassDegridTraceMColText);
+  const bool vlassDegridTraceThis =
+      !vlassDegridTraceDone && vlassDegridTracePath != nullptr &&
+      irow == vlassDegridTraceRow && ichan == vlassDegridTraceChannel &&
+      ipol == vlassDegridTracePol && static_cast<Int>(mCol) == vlassDegridTraceMCol;
+  std::ofstream vlassDegridTrace;
+
   Vector<Int> iCFPos(4,0);
   Int iLoc[2];
   Bool Dummy;
@@ -26,6 +48,42 @@
   phaseGradOrigin_l[0] = cached_phaseGrad_p.shape()(0)/2 ;
   phaseGradOrigin_l[1] = cached_phaseGrad_p.shape()(1)/2;
    
+  auto vlassFloatBits = [](Float value) {
+    std::uint32_t bits = 0;
+    static_assert(sizeof(bits) == sizeof(value), "Float32 trace width changed");
+    std::memcpy(&bits, &value, sizeof(bits));
+    return bits;
+  };
+  auto vlassWriteComplexBits =
+      [&](const char* label, const Complex& value) {
+        vlassDegridTrace << ' ' << label << "_re=" << vlassFloatBits(value.real())
+                         << ' ' << label << "_im=" << vlassFloatBits(value.imag());
+      };
+  if (vlassDegridTraceThis) {
+    vlassDegridTrace.open(vlassDegridTracePath, std::ios::out | std::ios::trunc);
+    if (!vlassDegridTrace) {
+      throw AipsError(
+          String("cannot open CASA_VLASS_DEGRID_PREFIX_TRACE output: ") +
+          vlassDegridTracePath);
+    }
+    vlassDegridTrace
+        << "meta"
+        << " row=" << irow
+        << " channel=" << ichan
+        << " pol=" << ipol
+        << " mcol=" << mCol
+        << " frequency_hz=" << std::setprecision(17) << freq[ichan]
+        << " data_w_m=" << std::setprecision(17) << dataWVal
+        << " loc_x=" << loc_ptr[0]
+        << " loc_y=" << loc_ptr[1]
+        << " support_x=" << support_ptr[0]
+        << " support_y=" << support_ptr[1]
+        << " sampling_x=" << sampling_ptr[0]
+        << " sampling_y=" << sampling_ptr[1]
+        << '\n';
+  }
+
+  std::size_t vlassTapIndex = 0;
   for(Int iy=-support_ptr[1]; iy <= support_ptr[1]; iy++) 
     {
       iLoc[1]    = SynthesisUtils::nint(sampling_ptr[1]*iy+off_ptr[1]);
@@ -40,22 +98,63 @@
 	  {
 	    wt=getFrom4DArray((const Complex * __restrict__ &)convFuncV,
 			      //igrdpos,cfInc_p);
 			      iCFPos_ptr,cfInc_p_ptr);
+	    const Complex vlassRawCF = wt;
 	    if (dataWVal <= 0.0) wt = conj(wt);
+	    const Complex vlassPostWCF = wt;
 	    norm(ipol)+=(wt);
+	    Complex vlassPhase(1.0F, 0.0F);
 	    if (finitePointingOffset) 
 	      {
-		wt *= conj(cached_phaseGrad_p(iLoc[0]+phaseGradOrigin_l[0],
-					      iLoc[1]+phaseGradOrigin_l[1]));
+                vlassPhase = cached_phaseGrad_p(
+                    iLoc[0]+phaseGradOrigin_l[0],
+                    iLoc[1]+phaseGradOrigin_l[1]);
+		wt *= conj(vlassPhase);
 	      }
 	    // nvalue+=wt*grid(grdpos);
 	    // The following uses raw index on the 4D grid
 	    // nvalue+=wt*getFrom4DArray(gridStore,iPosPtr,gridInc);
-	    nvalue+=wt*getFrom4DArray(gridStore,igrdpos_ptr,gridInc_p_ptr);
+            const Complex vlassGridValue =
+                getFrom4DArray(gridStore,igrdpos_ptr,gridInc_p_ptr);
+            const Complex vlassProduct = wt * vlassGridValue;
+	    nvalue += vlassProduct;
 	    //nvalue+=wt*getFrom4DArray(gridStore,igrdpos,gridInc_p);
+            if (vlassDegridTraceThis) {
+              vlassDegridTrace
+                  << "tap"
+                  << " index=" << vlassTapIndex
+                  << " iy=" << iy
+                  << " ix=" << ix
+                  << " cf_x=" << iCFPos_ptr[0]
+                  << " cf_y=" << iCFPos_ptr[1]
+                  << " grid_x=" << igrdpos_ptr[0]
+                  << " grid_y=" << igrdpos_ptr[1];
+              vlassWriteComplexBits("raw_cf", vlassRawCF);
+              vlassWriteComplexBits("post_w_cf", vlassPostWCF);
+              vlassWriteComplexBits("phase", vlassPhase);
+              vlassWriteComplexBits("post_phase_cf", wt);
+              vlassWriteComplexBits("grid", vlassGridValue);
+              vlassWriteComplexBits("product", vlassProduct);
+              vlassWriteComplexBits("norm", norm(ipol));
+              vlassWriteComplexBits("accumulator", nvalue);
+              vlassDegridTrace << '\n';
+            }
+            ++vlassTapIndex;
 	  }
 	}
     }
+  const Complex vlassPrePhasorValue = nvalue;
   nvalue = nvalue *conj(phasor);
+  if (vlassDegridTraceThis) {
+    vlassDegridTrace << "result";
+    vlassWriteComplexBits("pre_phasor", vlassPrePhasorValue);
+    vlassWriteComplexBits("phasor", phasor);
+    vlassWriteComplexBits("post_phasor", nvalue);
+    vlassWriteComplexBits("normalization", norm(ipol));
+    vlassWriteComplexBits("prediction", nvalue / norm(ipol));
+    vlassDegridTrace << '\n';
+    vlassDegridTrace.flush();
+    vlassDegridTraceDone = true;
+  }
 }
 //--------------------------------------------------------------------------------
