{
  "name": "@casa-rs/casars-assistant",
  "version": "0.24.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/protocol.test.js"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.80.2"
  },
  "devDependencies": {
    "@types/node": "24.12.4",
    "typescript": "5.9.3"
  }
}
